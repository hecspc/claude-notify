# Engineering bot notification

I want to be able to be notified everytime Claude Code is requesting an user input either because need some permission, or because it is finished or any other event from claude code.

I want to be notified to my telegram account using a telegram bot. The message needs to have:

- What kind of user input is requested
- What is the content of the request
- Session id or session identification in case the user have several sessions running in parallel

Create a plan of implementation with:

- The event from Claude
- The technology stack
- The architecture
- Environment variables to be used for configuration
- Steps of implementation

The plan should be save at ./plan.md

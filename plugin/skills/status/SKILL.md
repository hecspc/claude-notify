---
name: status
description: "Show claude-notify mute status — which sessions or global mute is active."
---

# Notification Status

Run `claude-notify status` to check what's currently muted.

Run the command, then show the user the output.
